[pytest]
# pythonpath = . puts backend/ on Python's import path so tests can do
# `from database import Base`, `from services.application import ...`, etc.,
# exactly the way the running app imports them.
pythonpath = .
# Only look for tests under tests/. Keeps pytest from scanning alembic/, .venv/.
testpaths = tests
